# v2post - V2EX 命令行发帖工具

通过 Chrome CDP 在 V2EX 一键发帖，支持 Markdown 格式。

## 使用

```bash
v2post -t "标题" -c "内容"              # 发到 share 节点
v2post -t "标题" -c "内容" -n python    # 发到指定节点
```

常用节点: `share`, `python`, `programmer`, `jobs`, `qna`, `apple`, `create`
